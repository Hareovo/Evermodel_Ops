# Copy to overrides.py for local-only settings. Environment variables are preferred.
import os

# Optional local filesystem/Grafana overrides; database and Redis use EVERMODEL_* env.
BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
REPOS_DIR = os.path.join(BASE_DIR, 'repos')
BUILD_DIR = os.path.join(REPOS_DIR, 'build')
GRAFANA_URL = os.environ.get('EVERMODEL_GRAFANA_URL', '')
GRAFANA_DASHBOARDS = ()
GRAFANA_TIMEOUT = 5
